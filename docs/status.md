# 시스템 현황

작성일: 2026-07-29
용도: 무엇을 재고, 무엇을 유도하고, 각각을 얼마나 믿을 수 있는지의 **기준 문서**.

설계 의도는 `docs/spatial-model.md`, 개념 정의는 `docs/spatial-primer.md`.
이 문서는 **현재 상태**만 다룬다. 새 값을 추가하거나 검증 상태가 바뀌면 여기를 고친다.

**신뢰 등급**은 문서 전체에서 아래 세 가지로만 쓴다.

| 등급 | 뜻 |
|---|---|
| **A** | 독립적인 방법으로 교차검증됨 |
| **B** | 그럴듯하고 내부적으로 일관되나 외부 검증 없음 |
| **C** | 알려진 결함이 있어 결론에 쓰면 안 됨 |

---

## 1. 파이프라인

### 1.1 단계와 인터페이스

| # | 스크립트 | 입력 | 출력 | 하는 일 |
|---|---|---|---|---|
| 1 | `track_boxers.py` | 영상, `config.json` | `tracks.csv` | 프레임별 화면 좌표 추출 |
| 2 | `calibrate_ring.py` | 영상, `tracks.csv`, 키 | `ground.json` | 카메라 보정 |
| 3 | `make_positions.py` | `tracks.csv`, `ground.json` | `positions.csv` | 바닥 좌표로 변환 |
| 4 | `make_stats.py` | `positions.csv` | `stats.md` / `.json` | 라운드 요약 |
| 5 | `make_topdown.py` | 영상, `positions.csv`, `ground.json` | `topdown.mp4` | 눈으로 검증 |

2단계가 1단계 출력을 입력으로 받는다 (사람 키로 카메라 높이를 정하는 데 필요).
즉 1 → 2 → 3 → {4, 5} 순서가 강제된다.

### 1.2 설계 원칙

| 원칙 | 구현 |
|---|---|
| 없는 값을 지어내지 않는다 | 못 믿는 프레임은 빈칸으로 남긴다. 커버리지가 낮아져도 그게 맞다 |
| 신뢰도를 함께 전달한다 | 각 단계가 신뢰도를 내보내고 다음 단계가 거른다 |
| 원시값과 유도값을 분리한다 | 재는 것은 5가지뿐. 나머지는 전부 계산 |
| 정답 라벨을 만들지 않는다 | 눈으로 보는 것이 검증 수단 |

---

## 2. 원시 측정값

영상에서 **직접 재는 것은 다음 5가지뿐이다.** 나머지는 모두 여기서 유도된다.

| ID | 값 | 출처 | 단위 | 등급 |
|---|---|---|---|---|
| `R1` | 발 위치 | 양 발목 키포인트 중점 (없으면 박스 아랫변) | 화면 px | A |
| `R2` | 어깨 좌·우 | 어깨 키포인트 2개 | 화면 px | A |
| `R3` | 손목 좌·우 | 손목 키포인트 2개 | 화면 px | B |
| `R4` | 상의 밝기 | 몸통 영역의 median L\* (CIELAB) | 0~255 | A |
| `R5` | 박스 겹침 | 두 사람 박스의 IoU | 0~1 | A |

모델: `yolo26n-pose` (COCO 17 키포인트).

### 2.1 정체 판별 — `R4` 기반

YOLO 추적 ID 를 쓰지 않는다. 실제 영상에서 **두 선수 모두 트랙이 끊겨 새 ID 를
받았고** (1→14, 3→47), 그것만 믿으면 두 명 동시 검출이 53.6% 였다.

대신 `R4` 로 매 프레임 독립 판단한다. 측정된 분포는 흰 상의 143~206, 검정 8~26.

| 판단 경로 | 조건 | IMG_0711 | IMG_0574 |
|---|---|---:|---:|
| 상대 비교 | 두 명 모두 보이고 밝기 차 ≥ 60 | 88.0% | 92.4% |
| 절대 임계 | 밝기 > 110 또는 < 45 | 7.4% | 5.0% |
| 직전 위치 | 위 둘 다 실패 | 4.6% | 2.6% |

검증: 두 영상 모두 `me` 의 밝기 중앙값이 17 / 27 (검정), `opponent` 가 180 / 203 (흰색).

### 2.2 후보 배제 규칙

| 규칙 | 기준 | 이유 |
|---|---|---|
| 링 밖 배제 | 발이 `ring_polygon` 밖 | 링 옆 관원이 상대로 잡힌 사고 |
| 순간이동 배제 | 45 px/프레임 초과 (하한 120px, 상한 600px) | 사람이 갈 수 없는 거리 |
| 최대 2명 | 박스 높이 상위 2개 | 선수가 카메라에 더 가깝다 |

---

## 3. 카메라 보정

`ground.json` 에 저장. 3~5단계가 모두 이 값에 의존한다.

### 3.1 유도 경로

```
배경 프레임 (60장 중앙값 → 사람 제거)
   └─ Hough 선분 검출
        └─ RANSAC 소실점 2개  ──┬─► C1 초점거리   (두 방향이 직각이라는 조건)
                                └─► C2 지평선     (두 소실점을 잇는 선)
                                       │
사람 키 (입력) ─────────────────────────┴─► C3 카메라 높이
                                       
소실점 + 캔버스 색 분할 ──────────────────► C4 링 경계
```

### 3.2 값과 검증

| ID | 값 | IMG_0711 | IMG_0574 | 등급 | 검증 방법 |
|---|---|---:|---:|---|---|
| `C1` | 초점거리 | 2249 px (42mm) | 4757 px (45mm) | **A** | 아래 3.3 |
| `C2` | 지평선 y | 74 | 190 | **A** | `C1` 과 함께 |
| `C3` | 카메라 높이 | 2.12 m | 2.20 m | **A** | 영상 간 8cm 일치 |
| `C4` | 링 한 변 | 3.63~3.94 m | 3.23~4.26 m | **C** | 불일치 (문제 P2) |

### 3.3 `C1` 교차검증

두 선수가 화면에서 **좌우로** 벌어져 있을 때의 거리는 초점거리와 무관하고,
**앞뒤로** 벌어져 있을 때는 비례한다. 복서는 카메라 방향과 무관하게 비슷한
거리를 유지하므로, 두 경우가 일치하는 초점거리가 정답이다.

| 방법 | 값 |
|---|---:|
| 소실점 (기하) | 2249 |
| 거리 자기일관성 (통계) | 2200 |
| 차이 | 2% |

`C3` 도 독립 검증됐다. 같은 삼각대로 찍은 두 영상에서 **따로 계산한 값이 8cm 차이**다.

### 3.4 이전에 틀렸던 것

초기에는 사람 키만으로 바닥 평면을 추정했고, **초점거리를 기종 명목값(1280)으로
가정**했다. 줌으로 찍힌 영상이라 그 가정이 틀렸고, 실제값은 1.76배인 2249 였다.

결과적으로 깊이가 절반 이하로 눌렸다. 링은 정사각형인데 선수 이동 범위가
좌우 3.58m × 앞뒤 1.55m (비율 0.43) 로 나왔다. 보정 후 3.27 × 2.71m (비율 0.83).

---

## 4. 유도값 (프레임 단위)

`positions.csv` 의 각 열. **전부 계산으로 나온다.**

| ID | 열 | 유도 | 의존 | 등급 |
|---|---|---|---|---|
| `D1` | `me_x, me_z` | `Z = C1·h/(R1y−C2)`, `X = (R1x−cx)·h/(R1y−C2)` | R1, C1~C3 | **A** |
| `D2` | `distance_m` | `D1` 두 점의 거리 | D1 | **A** |
| `D3` | `me_facing` | 두 어깨의 깊이 차이로 방향 (4.2) | R2, C1~C2 | **C** |
| `D4` | `me_facing_rel` | 어깨선 화면 기울기의 sin | R2 | **A** |
| `D5` | `me_reach` | 발~손목 좌우거리의 98% 분위수 | R3, C2~C3 | **C** |
| `D6` | `confidence` | 감점표 곱 (4.4) | R4, R5, R1 | **A** |

### 4.1 발목 높이 보정 (`D1`)

발목 키포인트는 지면이 아니라 **발목 관절**이다 (성인 키의 약 5%, 9cm).
그대로 투영하면 카메라 반대쪽으로 밀리며, 7m 거리에서 30cm 를 넘는다.
카메라 높이를 `h = C3 − 발목높이` 로 낮춰 투영해 고쳤다.

발견 경위: 링 경계와 겹쳐 보니 선수가 캔버스 밖 39cm 까지 나가 있었다.

### 4.2 정면 방향 (`D3`)

두 어깨는 같은 높이에 있으므로 각 어깨의 깊이를 따로 구해 방향을 만든다.
**어깨 높이는 계산에서 약분되므로 키를 몰라도 된다.**

시계열 처리를 두 갈래로 나눈다. 하나로 묶으면 서로를 망친다.

| 성분 | 처리 | 이유 |
|---|---|---|
| 축 (기울기) | 각도 2배 → 원형 평균 → 이어붙임 → 절반 | 부호가 뒤집힌 프레임이 섞여도 축 평균은 안 망가진다 |
| 부호 (앞/뒤) | 주변 창 다수결 + 관성 | 국소적이라 오류가 퍼지지 않는다 |

절반으로 되돌리기 전에 이어붙이지 않으면 결과가 (−90, 90] 에 갇혀 경계마다
180도씩 튄다. 회전이 아니라 표현의 문제다.

### 4.3 사거리 반경 (`D5`)

발 중심에서 손목까지의 **좌우 거리**, 영상 전체의 98% 분위수.
좌우 성분만 쓰는 이유는 세로 거리에는 손 높이가 섞이기 때문이다.

### 4.4 신뢰도 (`D6`)

| 요인 | 계수 |
|---|---|
| 정체 판별: 상대비교 / 절대임계 / 직전위치 | 1.0 / 0.85 / 0.4 |
| 발 위치: 양발목 / 한발목 / 박스 | 1.0 / 0.85 / 0.6 |
| 발이 화면 밖으로 잘림 | 0.5 |
| 박스 겹침 ≥ 0.15 / ≥ 0.35 | 0.7 / 0.25 |

거리가 0.3 m 미만이면 물리적으로 불가능하므로 값을 버린다 (평활화 **뒤에** 적용).

### 4.5 커버리지

| | IMG_0711 | IMG_0574 |
|---|---:|---:|
| 두 명 다 검출 | 88.5% | 90.9% |
| 거리가 나온 프레임 | 88.3% | 90.7% |
| 신뢰도 ≥ 0.6 | 80.9% | 82.8% |

---

## 5. 스탯 (라운드 단위)

`make_stats.py`. 신뢰도 0.6 미만 제외.

### 5.1 거리

| ID | 스탯 | 유도 | 등급 |
|---|---|---|---|
| `S1` | 거리 분포 | `D2` 의 중앙값·사분위·표준편차 | **A** |
| `S2` | 사거리 관계 | `D2` vs `D5`+몸반경, 4분할 | **C** |

`S2` 는 확정값을 내지 않고 **여유값을 바꿔가며 감도표**로만 낸다 (문제 P4).

### 5.2 움직임

속도는 ±3프레임 중앙차분. 6 m/s 초과는 추적 오류로 버린다.

**주도권 분해**가 이 층의 핵심이다. 두 사람의 속도를 서로를 잇는 축에 투영하면:

```
거리 변화율 = −( 내가 상대 쪽으로 간 속도 + 상대가 내 쪽으로 온 속도 )
```

각자의 기여가 분리되므로 "누가 거리를 정하는가" 가 수치가 된다.

| ID | 스탯 | 유도 | 등급 |
|---|---|---|---|
| `S3` | 좁힐 때 주도 | 거리 감소 프레임 중 내 기여가 큰 비율 | **B** |
| `S4` | 벌릴 때 주도 | 거리 증가 프레임 중 내가 더 물러난 비율 | **B** |
| `S5` | 전진 시간 | 내 속도의 상대 방향 성분 > 0 인 비율 | **B** |
| `S6` | 이동속도 | 속도 크기의 중앙값 | **B** |

### 5.3 각도

**`D4` ≥ 0.3 인 프레임만 사용.** 두 선수 동시 조건이라 실제 커버리지는 3%.

| ID | 스탯 | 유도 | 등급 |
|---|---|---|---|
| `S7` | 정면 벗어난 각 | `D3` 와 상대 방향의 차이 | **C** |
| `S8` | 각도 우위 시간 | 내 벗어난 각 < 상대인 비율 | **C** |
| `S9` | 피벗 빈도 | 0.3초에 45도 이상 회전, 불응기 0.4초 | **C** |

---

## 6. 측정 결과

| 스탯 | IMG_0711 | IMG_0574 | 등급 |
|---|---:|---:|---|
| `S1` 거리 중앙값 | 1.61 m | 1.77 m | A |
| `S3` 좁힐 때 내가 주도 | **24%** | **49%** | B |
| `S4` 벌릴 때 내가 주도 | 63% | 68% | B |
| `S5` 내가 전진 중인 시간 | 32% | 39% | B |
| `S6` 내 이동속도 | 0.69 m/s | 0.87 m/s | B |
| `S8` 각도 우위 시간 | 51% | — | C |

### 6.1 주요 관찰

**IMG_0711 의 뒷발 성향.** 거리를 좁힌 것은 76% 가 상대, 벌린 것은 63% 가 나,
전진 시간 32%. 세 지표가 같은 방향을 가리킨다.

2026-07-10 자기 리뷰의 "압박받으면 직선으로 밀린다" 와 **독립적으로 일치한다.**
영상을 보고 쓴 소감과 데이터가 같은 결론에 도달했다.

**단, IMG_0574 는 49% 로 대등하다.** 성향인지, 그날인지, 상대 탓인지 구분할 수 없다
(문제 P5).

---

## 7. 알려진 문제

| ID | 문제 | 영향 범위 | 원인 유형 | 해결책 |
|---|---|---|---|---|
| **P1** | 각도 측정 커버리지 3% | `D3`, `S7`~`S9` | 기하학적 한계 | 촬영 화각 |
| **P2** | 링 크기 불일치 | `C4`, 링 관련 스탯 | 알고리즘 | 로프 기반 검출 |
| **P3** | 사거리가 영상 길이에 좌우 | `D5`, `S2` | 정의 결함 | 2단계 펀치 이벤트 |
| **P4** | 거리 5단계 불가 | `S2` | P3 + 경계 무효 | P3 해결 후 |
| **P5** | 데이터 2 라운드 | 모든 결론 | 표본 부족 | 촬영 누적 |

### P1 — 각도 측정 커버리지 3%

**버그가 아니라 기하학적 한계다.** 어깨선이 화면에서 수평에 가까우면 3차원 방향이
원래 결정되지 않는데, 이 카메라 각도에서는 **71% 의 프레임이 그렇다** (`D4` 중앙값 0.10).
카메라가 링을 얕게 내려다보고 복서 어깨는 대체로 수평이기 때문이다.

`D4` 로 거르면 값은 개선된다.

| `D4` 하한 | 남는 프레임 | 상대향 오차 | 90도 이내 |
|---:|---:|---:|---:|
| 0 | 99% | 33도 | 90% |
| 0.3 | 20% | 26도 | 96% |
| 0.5 | 11% | 26도 | 98% |

그러나 **두 선수가 동시에 신뢰할 만한 프레임은 3%** (0.2 × 0.2). 99초 중 3초다.

부수 위험: `S9`(피벗)는 이 필터와 상성이 나쁘다. 몸을 돌리는 동안 어깨선이 수평
구성을 지나가므로, 정작 피벗하는 순간이 걸러질 수 있다.

> **진단 과정에서 두 번 틀렸다.**
> ① 지표를 잘못 봤다 — 프레임당 변화 1.3도만 보고 좋다고 판단했으나,
>    실제로는 방향 전환이 초당 7~10회였다. 작은 변화가 부호를 계속 바꾸면 떨림이 된다.
> ② 원인을 잘못 짚었다 — 식이 발산한다고 보고 바꿨으나 두 식은 같은 값이었다
>    (차이 중앙값 0.0000도). 대수적으로 동일한 식은 입력 잡음을 똑같이 전달한다.
>    조건수는 부동소수점 정밀도의 문제이지 측정 잡음 민감도의 문제가 아니다.

### P2 — 링 크기 불일치

같은 링인데 방법을 바꿀 때마다 값이 바뀐다.

| 시도 | IMG_0711 | IMG_0574 |
|---|---:|---:|
| 고정 색 기준 | 3.91 m | 3.37 m |
| 발 위치로 색 자동 맞춤 | 3.94 m | 3.70 m |
| + 선수 범위로 제한 | 3.94 m | 4.26 m |
| + 발이 닿은 덩어리만 | 3.63 m | 3.23 m |

원인이 복합적이다. 캔버스 색이 영상마다 다르고, 어떤 영상은 링 밖 바닥과 색이
거의 같고, 로프가 캔버스를 가로로 가르며, 이어붙이는 연산이 배경으로도 다리를 놓는다.

**영향 범위가 제한적이다.** `C4` 는 top-down 테두리 그리기에만 쓰인다.
`D1`·`D2` 는 `C1`~`C3` 에서 나오고 그것은 등급 A 다.

### P3 — 사거리가 영상 길이에 좌우

같은 사람인데 0.65 m (표본 3924) vs 0.53 m (표본 912). 98% 분위수를 쓰는데
짧은 영상에서는 최대 신전 순간이 안 잡힌다. **영상 길이에 따라 달라지는 정의는
정의가 아니다.**

### P4 — 거리 5단계 불가

두 가지가 겹쳤다.

- 밀착 경계 0.65 m 는 **틀린 보정 위에서** 정한 값이라 무효
- **한 발 사거리**가 없다. 정지 사거리만으로는 99% 가 "둘 다 못 닿음"

primer 정의가 이미 그렇게 말한다 — "엣지 오브 레인지 = **한 발 들어가면** 닿는 경계".
원이 몸 / 정지 사거리 / 한 발 사거리 **세 개**여야 한다.

### P5 — 표본 부족

모든 결론이 라운드 하나씩에 기반한다.

---

## 8. 다음 작업

| 우선 | 작업 | 푸는 문제 | 비용 |
|---|---|---|---|
| 1 | **촬영 화각 변경** (카메라 높이기) | **P1 + P2 동시** | 계산 몇 개 |
| 2 | 2단계 펀치 이벤트 | P3 + P4 | 큼 (기존 검출기 있음) |
| 3 | 로프 기반 링 검출 | P2 | 중간 |
| 4 | 영상 누적 | P5 | 시간 |

**1번이 가장 효율적이다.** 카메라를 높이면 어깨선이 화면에서 기울어져 `D4` 가
올라가고(P1), 동시에 링 전체가 화면에 들어와 경계 검출이 쉬워진다(P2).
코드 수정이 아니라 촬영 조건 변경이므로 비용도 가장 낮다.

---

## 9. 현재 사용 가능한 범위

| 층 | 등급 | 사용 가능 여부 |
|---|---|---|
| 거리 | **A** | 사용 가능 |
| 움직임 | **B** | 사용 가능 |
| 링 | **C** | 사용 불가 (P2) |
| 각도 | **C** | 사용 불가 (P1) |

거리와 움직임만으로 고전적 스타일 두 축(**선호 거리** + **거리 주도권**)이 구성된다.
`docs/style-project.md` 에서 정한 MVP 범위와 일치하므로, 현재 상태로도
스타일 분석의 최소 형태는 가능하다.
